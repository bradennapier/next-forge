{
  "name": "@repo/payments",
  "version": "0.0.0",
  "dependencies": {
    "@repo/env": "workspace:*",
    "@stripe/agent-toolkit": "catalog:",
    "stripe": "catalog:"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "catalog:"
  },
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "format": "biome lint --write .",
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  }
}
